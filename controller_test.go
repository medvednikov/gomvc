package ezweb

import "testing"

func TestActionName(t *testing.T) {
	/*
		inputs := []string{"Home", "Account/Register", "", "Login", "Forum/Topic/Hello-world"}
		outputs := []string{"Home", "Register", "Index", "Login", "Topic"}
		argsExpected := [][]string{[]string{}, []string{}, []string{}, []string{}, []string{"Hello-world"}}

		for i, input := range inputs {
			action, args := getActionAndArgsFromUri(input, false)
			if action != outputs[i] {
				t.Errorf("Expected '%s', got '%s'", outputs[i], action)
			}

			if !reflect.DeepEqual(args, argsExpected[i]) {
				t.Errorf("Wrong args. Expected: %s. Got: %s", argsExpected[i], args)
			}
		}
	*/
}
